*javascript is loosly typing, it is when the language, assignes the types to variables, without we doing anything.

let a:number =1
* a, can only be a number, and nothing else

function sum(a:number,b:number) : number{
    return a+b
}

sum(1,2) //valid
sum('one')
